function value=DTW(p, t)
	DTW=zeros(size(p,2),size(t,2));
	for i=1:size(p,2)
		DTW(i,1)=inf;
	end
	for j=1:size(t,2)
		DTW(1,j)=inf;
	DTW(1,1)=0;
	end
	
	for i=1:size(p,2)
		for j=1:size(t,2)
			dist=abs(p(i)-t(j));
			DTW(i,j)
end
